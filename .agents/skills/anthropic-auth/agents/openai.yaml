interface:
  display_name: "Anthropic Auth"
  short_description: "Repo-specific patterns and workflows for anthropic-auth"
  default_prompt: "Use the anthropic-auth repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true