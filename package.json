{
  "name": "@cortexkit/anthropic-auth",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "rm -rf dist && cd packages/core && bun run build && cd ../opencode && bun run build && cd ../pi && bun run build",
    "dev": "bun scripts/dev.ts",
    "dev:clean": "bun scripts/dev-clean.ts",
    "extract": "bun scripts/extract-system-prompt.ts",
    "check:claustrum-golden": "bun scripts/check-claustrum-golden.ts",
    "check:pi-dist-imports": "bun scripts/check-pi-dist-imports.ts",
    "check:workspace-lock": "node scripts/version-sync.mjs --check-lock",
    "analyze:cache": "node scripts/analyze-cache-usage.mjs",
    "test": "bun run build && bun run check:pi-dist-imports && cd packages/core && bun test src/tests && cd ../opencode && bun run test && cd ../pi && bun run test && cd ../.. && bun run test:pi-host",
    "test:pi-host": "bun scripts/check-pi-tool-mapping.ts",
    "test:e2e": "cd packages/core && bun run build && cd ../.. && bun run --cwd packages/e2e-tests test",
    "typecheck": "cd packages/core && bun run build && bun run typecheck && cd ../opencode && bun run typecheck && cd ../pi && bun run typecheck && cd ../e2e-tests && bun run typecheck && cd ../.. && tsc -p tsconfig.scripts.json",
    "types": "bun run typecheck",
    "format": "biome check --write --unsafe .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "pack:core:dry": "cd packages/core && bun pm pack --dry-run",
    "pack:opencode:dry": "cd packages/opencode && bun pm pack --dry-run",
    "pack:pi:dry": "cd packages/pi && bun pm pack --dry-run",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@babel/parser": "7.29.7",
    "@biomejs/biome": "2.5.14",
    "@earendil-works/pi-ai": "0.86.1",
    "@earendil-works/pi-coding-agent": "0.86.1",
    "@earendil-works/pi-tui": "0.86.1",
    "@opencode-ai/plugin": "1.18.31",
    "@tsconfig/bun": "1.0.11",
    "@types/bun": "1.4.2",
    "dedent": "^1.7.2",
    "jsonc-parser": "^3.3.1",
    "lefthook": "2.1.14",
    "miniflare": "^4.20260730.0",
    "typescript": "7.0.2"
  }
}
